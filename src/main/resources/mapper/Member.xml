<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.megacoffee.modules.member.MemberRepository">
  <insert id="appendCommute" parameterType="com.megacoffee.modules.member.CommuteVO" useGeneratedKeys="true" keyProperty="seq" keyColumn="seq">
    INSERT INTO TB_COMMUTE
    (
      HEADER_DETAILS, MAC_ADDRESS, OS, REMOTE_ADDRESS
      , USER_AGENT, SESSION_ID
      , CREATE_DATETIME
    )
    VALUES
    (
      #{headerDetails}, #{macAddress}, #{os}, #{remoteAddress}
      , #{userAgent}, #{sessionId}
      , NOW()
    )
  </insert>

  <select id="checkCommute" statementType="CALLABLE" parameterType="com.megacoffee.modules.member.CommuteVO">
    { CALL test.PROC_APPEND_COMMUTE(
        #{headerDetails, mode=IN, jdbcType=VARCHAR},
        #{macAddress, mode=IN, jdbcType=VARCHAR},
        #{os, mode=IN, jdbcType=VARCHAR},
        #{remoteAddress, mode=IN, jdbcType=VARCHAR},
        #{userAgent, mode=IN, jdbcType=VARCHAR},
        #{sessionId, mode=IN, jdbcType=VARCHAR},
        #{userSeq, mode=IN, jdbcType=BIGINT},
        #{resultCode, mode=OUT, jdbcType=CHAR}
    )}
</select>
</mapper>
