<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.megacoffee.infra.FileRepository">

  <select id="findByFileId" resultType="com.megacoffee.model.FileVO" parameterType="String">
    SELECT 
        SEQ, FILE_ID
        , ORIGINAL_NAME, STORED_NAME
        , FILE_PATH, FILE_EXTENSION
        , FILE_SIZE, MIME_TYPE
        , CREATE_IDX, CREATE_DATETIME
        , UPDATE_IDX, UPDATE_DATETIME
    FROM TB_FILE
    WHERE FILE_ID = #{fileId}
      AND DELETED = 0
  </select>

  <insert id="append" parameterType="com.megacoffee.model.FileVO" useGeneratedKeys="true" keyProperty="seq" keyColumn="SEQ">
    INSERT INTO TB_FILE
    (
      FILE_ID, ORIGINAL_NAME, STORED_NAME
      , FILE_PATH, FILE_EXTENSION
      , FILE_SIZE, MIME_TYPE
      , CREATE_IDX, CREATE_DATETIME
    )
    VALUES
    (
      #{fileId}, #{originalName}, #{storedName}
      , #{filePath}, #{fileExtension}
      , #{fileSize}, #{mimeType}
      , #{createIdx}, NOW()
    )
  </insert>

</mapper>
